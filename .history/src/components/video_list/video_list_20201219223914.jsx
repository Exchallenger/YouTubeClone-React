import React from 'react';

const VideoList = (props) => {
            const a = props.video;
            // a.array.forEach(element => {
                
            // });
            console.log(a);
            return <h1>halo</h1>
};

export default VideoList;