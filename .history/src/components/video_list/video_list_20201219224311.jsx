import React from 'react';

const VideoList = (props) => {
            const a = props.video.snippet;
            console.log(a);
            // a.map((el)=>{
            //     let obj = {};
            //     obj[el.key] = el.value;
            //     return obj;
            // })

            return <h1>halo</h1>
};

export default VideoList;